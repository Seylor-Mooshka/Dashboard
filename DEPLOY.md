# 🚀 Инструкция по деплою на GitHub Pages

## Шаги для публикации проекта:

### 1. Создание репозитория на GitHub

1. Перейдите на [GitHub.com](https://github.com)
2. Нажмите кнопку "New repository" (зеленая кнопка)
3. Заполните форму:
   - **Repository name**: `dashboard` (или любое другое имя)
   - **Description**: "Аналитический дашборд с виджетами"
   - **Visibility**: Public (для бесплатного GitHub Pages)
   - **Initialize**: НЕ ставьте галочки (у нас уже есть файлы)
4. Нажмите "Create repository"

### 2. Подключение локального репозитория к GitHub

Выполните команды в терминале (замените `your-username` на ваш GitHub username):

```bash
# Добавляем удаленный репозиторий
git remote add origin https://github.com/your-username/dashboard.git

# Переименовываем ветку в main (современный стандарт)
git branch -M main

# Отправляем код на GitHub
git push -u origin main
```

### 3. Настройка GitHub Pages

1. Перейдите в настройки репозитория:
   - Откройте ваш репозиторий на GitHub
   - Нажмите вкладку "Settings"
   - Прокрутите вниз до раздела "Pages"

2. Настройте источник:
   - **Source**: Deploy from a branch
   - **Branch**: main
   - **Folder**: / (root)
   - Нажмите "Save"

3. Дождитесь деплоя:
   - GitHub автоматически создаст сайт
   - Обычно это занимает 1-2 минуты
   - URL будет: `https://your-username.github.io/dashboard`

### 4. Проверка работы

1. Откройте URL вашего сайта
2. Проверьте все функции:
   - Добавление виджетов
   - Работу с ToDo списком
   - Загрузку цитат
   - Отображение погоды и криптовалют
   - Сохранение состояния

### 5. Обновление сайта

При внесении изменений в код:

```bash
# Добавляем изменения
git add .

# Создаем коммит
git commit -m "Описание изменений"

# Отправляем на GitHub
git push origin main
```

GitHub Pages автоматически обновит сайт через несколько минут.

## 🔧 Дополнительные настройки

### Кастомный домен (опционально)

Если у вас есть собственный домен:

1. В настройках Pages добавьте ваш домен
2. Создайте файл `CNAME` в корне проекта:
   ```
   your-domain.com
   ```
3. Настройте DNS записи у вашего провайдера

### HTTPS (автоматически)

GitHub Pages автоматически предоставляет HTTPS сертификат.

## 🐛 Решение проблем

### Сайт не обновляется
- Подождите 5-10 минут
- Очистите кэш браузера (Ctrl+F5)
- Проверьте статус деплоя в разделе "Actions"

### Ошибки в консоли
- Проверьте, что все файлы загружены
- Убедитесь, что пути к файлам корректны
- Проверьте CORS политики для API

### API не работают
- Некоторые API могут блокировать запросы с GitHub Pages
- Используйте CORS прокси или настройте собственный сервер
- Для демо используются моковые данные

## 📱 Тестирование

Проверьте работу на разных устройствах:
- Десктоп (Chrome, Firefox, Safari)
- Мобильные устройства
- Планшеты

## 🎯 Финальная проверка

Убедитесь, что:
- [ ] Сайт доступен по URL
- [ ] Все виджеты добавляются
- [ ] ToDo список сохраняется
- [ ] Цитаты загружаются
- [ ] Дизайн адаптивный
- [ ] Нет ошибок в консоли

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи в консоли браузера
2. Убедитесь, что все файлы загружены
3. Проверьте настройки GitHub Pages
4. Обратитесь к документации GitHub Pages

---

**Успешного деплоя! 🚀**
